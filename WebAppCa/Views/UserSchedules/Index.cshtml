@model IEnumerable<WebAppCa.Models.UserSchedule>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Schedule)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Schedule.ScheduleId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserScheduleID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserScheduleID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserScheduleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
